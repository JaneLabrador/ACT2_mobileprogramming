import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const LandingScreen = () => {
  return (
    <SafeAreaView>
      <Text>LandinsScreen</Text>
    </SafeAreaView>
  );
};

export default LandingScreen;
